# GroundPlane Persistence Policy Data Extension

Pure contract package for data classes, memory-default profiles,
store capability descriptors, durable opt-in preflights, and debug redaction
helpers.

This package does not depend on databases, durable substrates, external SDKs,
object stores, vector stores, higher-layer repos, or LiveView.

## Persistence Documentation

See `docs/persistence.md` for tiers, defaults, adapters, unsupported selections, config examples, restart claims, durability claims, debug sidecar behavior, redaction guarantees, migration or preflight behavior, and no-bypass scope when applicable.
