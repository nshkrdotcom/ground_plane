# Internal Libraries

The initial internal slices are:

- `ground_plane_contracts`
- `ground_plane_postgres`
- `ground_plane_projection`

They are listed here to make the intended shape explicit even before the full
implementation lands.
